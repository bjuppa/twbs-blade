<input type="{{ $type or 'text' }}" name="{{ $name }}" class="form-control" id="{{ $control_id }}" aria-describedby="{{ $control_description_id }}"
       value="{{ old($name, isset($value) ? $value : null) }}">